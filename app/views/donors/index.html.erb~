<h1>Listing Donors</h1>

<table>
  <tr>
    <th>Company</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @donors.each do |donor| %>
  <tr>
    <td><%= link_to donor.name, donor %></td>
    <td><%= link_to 'Details', donor %></td>
    <td><%= link_to 'Destroy', donor, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Donor', new_donor_path %>
