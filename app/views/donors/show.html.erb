<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @donor.name %>
</p>

<p>
  <b>Email:</b>
  <%= @donor.email %>
</p>

<%= link_to 'New Donation', new_donation_path(@donor) %> |
<%= link_to 'Edit', edit_donor_path(@donor) %> |
<%= link_to 'Back', donors_path %>
